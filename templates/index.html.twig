{% extends 'base.html.twig'%}

{% block title %} Accueil {% endblock %}
{% block body %}
    {% for post in posts %}

        <h1>{{post.title}} </h1>
        <p>{{post.body}}</p>

    {% endfor %}
{% endblock %}